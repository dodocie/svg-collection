# SVG Collection 启动指南

## 项目概述
SVG 特效展示项目，基于 React + TypeScript + Vite 构建，包含动画、渐变、滤镜、路径变形等多种 SVG 效果演示。

## 快速启动

```bash
npm run dev
```

```yaml
subProjectPath: .
command: npm run dev
cwd: .
description: SVG 特效展示项目，包含动画、渐变、滤镜、路径变形等效果
```
